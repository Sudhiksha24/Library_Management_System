CREATE TABLE AUTHOR_TBL (
    AUTHOR_CODE NUMBER PRIMARY KEY,
    AUTHOR_NAME VARCHAR2(50) NOT NULL,
    CONTACT_NO  NUMBER(10)
);
INSERT INTO AUTHOR_TBL VALUES (101, 'Robin Sharma', 9876543210);
INSERT INTO AUTHOR_TBL VALUES (102, 'R.K Narayan', 9876543211);
INSERT INTO AUTHOR_TBL VALUES (103, 'Paulo Coelho', 9876543212);

COMMIT;
CREATE TABLE BOOK_TABLE (
    ISBN        VARCHAR2(20) PRIMARY KEY,
    BOOK_TITLE  VARCHAR2(100) NOT NULL,
    BOOK_TYPE   CHAR(1),
    AUTHOR_CODE NUMBER,
    BOOK_COST   NUMBER(8,2),
    CONSTRAINT FK_AUTHOR
        FOREIGN KEY (AUTHOR_CODE)
        REFERENCES AUTHOR_TBL(AUTHOR_CODE)
);
SELECT * FROM AUTHOR_TBL;
SELECT * FROM BOOK_TABLE;
